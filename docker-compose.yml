services:
  smoogle:
    image: wyattbrashear48517/smoogle:latest
    container_name: smoogle
    restart: unless-stopped
    environment:
      - DB_PATH=/app/data/SearchData.db
    ports:
      - "5000:5000"
    volumes:
      - smoogle_data:/app/data

volumes:
  smoogle_data:
